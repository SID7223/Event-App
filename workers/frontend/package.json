{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build-web": "cd ../.. && npx expo export --platform web --output-dir web-build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250701.0",
    "wrangler": "^4.0.0"
  }
}
